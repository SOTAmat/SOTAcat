# Prerequisites
*.d

# Object files
*.o
*.ko
*.obj
*.elf

# Linker output
*.ilk
*.map
*.exp

# Precompiled Headers
*.gch
*.pch

# Libraries
*.lib
*.a
*.la
*.lo

# Shared objects (inc. Windows DLLs)
*.dll
*.so
*.so.*
*.dylib

# Executables
*.exe
*.out
*.app
*.i*86
*.x86_64
*.hex

# Debug files
*.dSYM/
*.su
*.idb
*.pdb

# Kernel Module Compile Results
*.mod*
*.cmd
.tmp_versions/
modules.order
Module.symvers
Mkfile.old
dkms.conf

# Original .gitignore from PlatformIO
.fake
.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# emacs / LSP / platformio
.cache
.ccls-cache
compile_commands.json

# configs especially volatile during development
dependencies.lock

# Used by the ESP-IDF build system to store configuration options such as boot logging, etc.
# We store our custom setting overrides in sdkconfig.default, and should commit it to git
# The system will use that to produce transient files like sdkconfig.seeed_xiao_esp32c3_{debug,release}.  Those we can ignore.
sdkconfig.*
!sdkconfig.defaults
!sdkconfig.defaults.*

# ESP-IDF managed_compoments
managed_components/

__pycache__/
*.pyc

# Python virtual environment (created by 'make setup' in test/integration/)
.venv/
venv/

# Integration test results (generated by test runs)
test/integration/*.json
test/integration/baseline_*.json
test/integration/test_results_*.json
test_results/
webserver_test_results.json

# Pre-compressed web assets (generated at build time)
src/web/*.htmlgz
src/web/*.jsgz
src/web/*.cssgz

# Gzip-compressed web assets (build artifacts from kowalski/wip branch)
src/web/*.*gz

merged-firmware.bin
esp32c3.bin
esp32.bin
SOTACAT-ESP32C3-OTA.bin

# Gzip-compressed web assets (build artifacts from kowalski/wip branch)
src/web/*.*gz

